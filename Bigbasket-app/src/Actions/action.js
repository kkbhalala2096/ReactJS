import { fetchProduct, createProduct, deleteProduct, singleProduct, updateProduct, createUser } from "../Actions/index";

var actions = { fetchProduct, createProduct, deleteProduct, singleProduct, updateProduct, createUser };
export default actions;
